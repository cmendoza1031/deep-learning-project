# MedHetLoRA Requirements (Python 3.10–3.12)
# For local dev with Kaggle data — flamby not needed (Python 3.12 incompatible)
torch>=2.1.0
torchvision>=0.16.0
timm>=0.9.12
peft>=0.7.0
transformers>=4.36.0
numpy>=1.24.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
seaborn>=0.12.0
pandas>=2.0.0
gradio>=4.0.0
tqdm
pyyaml

# Flamby (FedISIC) — install ONLY in Colab (Python 3.10):
#   pip install 'flamby[fed_isic2019]'
# Flamby requires Python <3.11; skip for local Python 3.12
